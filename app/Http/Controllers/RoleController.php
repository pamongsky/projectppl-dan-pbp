<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;

class RoleController extends Controller
{
    // public function pilihRole(Request $request)
    // {
    //     $role = $request->input('role');  // Mengambil role yang dipilih
    //     session(['role' => $role]);  // Menyimpan role ke session
    
    //     return redirect()->route('redirectToDashboard');  // Mengarahkan ke dashboard sesuai role yang dipilih
    // }
}
